#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int number = get_int("Enter a number: ");
    for (int i = 0; int < 10; i++)
    {
        int artadryal = i*number;
        printf("%d x %d = %d\n", i, number, artadryal);
    }
}